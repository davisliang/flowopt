Research what works for this task before any workflow is designed:
${description}

Use the workflow-research skill. Search the web (this is required), read as many
sources as you need to understand how this kind of task is best approached, and
write your findings to research_notes.md in the working directory. The design
agent that runs next will build on those notes, so make them concrete and
specific to THIS task.

The workflows will route between these models (all served via OpenRouter), with
prices and Artificial Analysis capability indices where measured:
${models}

As part of the research, also look up how THESE models compare in practice —
independent evaluations, community experience, provider notes: which are strong
or weak at this kind of task, which are unreliable at instruction-following or
formatting, which cheap model punches above its price here. Record what you find
in research_notes.md next to the task findings, so the designer can decide which
model does which step from evidence rather than reputation.

Do not design or test any workflows — your only output is research_notes.md.
Write it and stop.
