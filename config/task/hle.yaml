# Full text-only HLE, imported by scripts/import_hle.py from cais/hle — the
# same 2,158-question subset Artificial Analysis evaluates, split into equal
# thirds (719 train / 719 val / 720 test, seeded).
task:
  name: hle
  description: >-
    Answer a Humanity's Last Exam question — expert-level, across many fields.
    Answers are short and specific; an answer is correct only if it matches the
    reference exactly (binary equality check with small numerical tolerance,
    no partial credit).
  check_type: llm_equality
  dataset: benchmarks/hle/data.jsonl

# Match Artificial Analysis's grading: GPT-4o (Aug) running the official HLE
# equality-checker prompt. Scores are percentage correct, comparable to AA's
# HLE numbers up to sampling of the splits.
judge:
  model: openai/gpt-4o-2024-08-06
